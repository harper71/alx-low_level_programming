#include <stdio.h>
/**
 * main - the main function
 * @argc: the argument count
 * @argv: the argument vector
 * Return: 0 always
 */

int main(int argc, char *argv[])
{

	printf("%d\n", argc);
	(void)argv;
	return (0);
}
